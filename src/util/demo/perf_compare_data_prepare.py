
# return a df with col 'date', 'baseline', 'experiment'
# no null, om date range
def perf_vs_benchmark(
    baseline_ticker,
    experiment_ticker,
    start_date,
    end_date,
    result_folder
):
    # download
    
    return



    